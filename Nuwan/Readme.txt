
// ------------------------------------------------------------------------------------------
// This is working without error, but no way to test because receiver is not working

To run the transmitter,
   javac Server.java && java Server

Next,
   1: Enable transmission
   0: Stop transmission

// ------------------------------------------------------------------------------------------
// This is half implemented, have a problem on buffer length, but I have no idea how it works

To run the receiver,
   javac Client.java && java Client
